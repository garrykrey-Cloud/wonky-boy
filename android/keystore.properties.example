# Copy to android/keystore.properties and fill in with YOUR OWN values.
# That file is gitignored and must never be committed or shared.
#
# storeFile should be an ABSOLUTE path to a keystore kept OUTSIDE this project,
# so it is never swept into a zip, a backup or a repo by accident.
#
# Create the keystore first - see PLAY-STORE.md section 3 for the command.

storeFile=C:/Users/garry/keys/wonky-boy-upload.jks
storePassword=CHANGE_ME
keyAlias=wonky-boy
keyPassword=CHANGE_ME
